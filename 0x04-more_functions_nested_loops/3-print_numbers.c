#include "main.h"

/**
 * print_numbers - check thr code for betty School students.
 *
 * Return: zero
 */
void print_numbers(void)
{
	ont a;

	for (a = o; a <= 9; a++)
	{
		_putchar('0' + a);
	}
	_putchar('\n);
}
